#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Cells
{
    float4x4 view_projection;
    float4 cell_size;
    uint4 _layout;
};

struct main0_out
{
    float4 _entryPointOutput_colour [[user(locn0)]];
    float4 gl_Position [[position]];
};

struct main0_in
{
    float4 input_colour [[attribute(0)]];
};

vertex main0_out main0(main0_in in [[stage_in]], constant Cells& _35 [[buffer(0)]], uint gl_VertexIndex [[vertex_id]], uint gl_InstanceIndex [[instance_id]])
{
    main0_out out = {};
    uint _167 = _35._layout.x * _35._layout.x;
    uint _173 = _35._layout.z + (gl_InstanceIndex / _167);
    uint _176 = gl_InstanceIndex % _167;
    out._entryPointOutput_colour = in.input_colour;
    out.gl_Position = _35.view_projection * float4((float2(float(((_173 % _35._layout.y) * _35._layout.x) + (_176 % _35._layout.x)), float(((_173 / _35._layout.y) * _35._layout.x) + (_176 / _35._layout.x))) + float2(float(gl_VertexIndex & 1u), float(gl_VertexIndex >> 1u))) * _35.cell_size.x, 0.0, 1.0);
    return out;
}

